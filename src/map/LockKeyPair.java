package map;


/**
 *   1. Create hash set of characters
 *   2. Iterate over the lock array and add the value in hash set directly
 *   3. Iterate over the key array and check key value exists in hash set ot not
 *     3.1 If exists, -> print lock and key
 *     3.2 else -> not valid input
 */
public class LockKeyPair {
}
