package stacks;


/**
 * 1. Create a stack of characters
 * 2. Iterate over the string which is exp.
 *   2.1. take the current character
 *   2.2.  if current character is not closing parenthesis
 *       2.2.1 push it in stack
 *   2.3 else keep pooping the elements from stack till you reach '('
 *       and at the same time maintains the count which will tell how many elements
 *       we have removed from stack
 *
 *   2.4  If(count <= 0 ) {
 *       return true;
 *   }
 *
 * 3. After iteration of string is done we will return false;
 *
 */
public class DuplicateParenthesis {



}
